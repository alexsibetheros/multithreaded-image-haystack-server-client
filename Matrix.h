#ifndef matrix
#define matrix
////////////////////////////////////////////////////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////////////////////////////
struct Matrix{
	int *Table;
	int Size;
};

typedef struct Matrix * MatrixPtr;
////////////////////////////////////////////////////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////////////////////////////

int InitiateMatrix(MatrixPtr *Matrix,int Size);
int DeleteMatrix(MatrixPtr *Matrix);
int AddMatrix(MatrixPtr Matrix,int Position,int Offset);
int PrintMatrix(MatrixPtr Matrix);
int GetElementMatrix(MatrixPtr Matrix,int Position);
int RemoveMatrix(MatrixPtr Matrix,int Position);

#endif /*matrix*/
